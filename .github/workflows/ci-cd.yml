name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
permissions:
  contents: write
  id-token: write
  issues: write
  pull-requests: write
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
    needs:
      - lint
      - build
      - test
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@749673fac018f205c652ee8520c9187cd8401cfe # v8.0.18
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
