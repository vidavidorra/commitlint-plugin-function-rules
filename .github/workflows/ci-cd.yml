name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@a3d26120430d11d1f311f8fd045028ddb037d27e # v5.0.44
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
