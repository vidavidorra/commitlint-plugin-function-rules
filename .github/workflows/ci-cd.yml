name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@c8d544c1f14aacac09362f24a53fcb68720c5355 # v6.0.15
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
