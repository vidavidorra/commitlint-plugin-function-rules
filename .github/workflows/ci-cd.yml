name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@fb60eab7f7b03af3ccbd56a089785c9435500098 # v6.0.10
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
