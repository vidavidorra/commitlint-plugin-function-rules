name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@fb2f15c5e610ef5d08b096c6a58573f80170751e # v5.0.60
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
