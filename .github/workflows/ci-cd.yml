name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@ea4928cfcf27f1a88697855c59dc312e64fb732d # v6.0.9
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
