name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 23]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 23]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@eac505274edf1cd7bcda4b555714c2edb22a457b # v5.0.35
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
