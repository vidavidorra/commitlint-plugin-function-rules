name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 23]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 23]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@560f9d8f9de4c3e0de105fd071564fea60549eba # v5.0.36
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
