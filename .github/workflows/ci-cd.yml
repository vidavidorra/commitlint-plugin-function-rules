name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@3122bec6e67b139db2acd23c02344b55de0f2adc # v6.0.4
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
