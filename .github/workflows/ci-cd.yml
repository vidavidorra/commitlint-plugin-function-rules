name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 21]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 21]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@e104d4cfe844fabb216d580821b381c20b867668 # v4.2.16
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
