name: CI/CD
on:
  push:
    branches:
      - main
      - beta
      - renovate/**
  pull_request: null
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
  lint:
    uses: vidavidorra/.github/.github/workflows/node-lint.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
  build:
    uses: vidavidorra/.github/.github/workflows/node-build.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  test:
    uses: vidavidorra/.github/.github/workflows/node-test.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
    strategy:
      fail-fast: false
      matrix:
        nodeVersion: [20, 22, 24]
    with:
      nodeVersion: ${{ matrix.nodeVersion }}
  code-coverage:
    uses: vidavidorra/.github/.github/workflows/node-test-coverage.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
    needs:
      - lint
      - build
      - test
    secrets:
      codecovToken: ${{ secrets.CODECOV_TOKEN }}
  release:
    uses: vidavidorra/.github/.github/workflows/release.yml@ea829596fd96e8912355778a7e0f08f50f5b769c # v5.0.71
    needs:
      - lint-commit-messages
      - lint
      - build
      - test
      - code-coverage
    secrets:
      privateKey: ${{ secrets.RELEASE_PRIVATE_KEY }}
      npmToken: ${{ secrets.NPM_PUBLISH_TOKEN }}
