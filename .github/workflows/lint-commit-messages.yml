name: Lint commit messages
on:
  push:
    branches:
      - main
      - beta
      - 'renovate/**'
  pull_request:
jobs:
  lint-commit-messages:
    uses: vidavidorra/.github/.github/workflows/lint-commit-messages.yml@dc51b746f22c1852be005d71f102feb29cf425cf # v2.2.3
    with:
      configFile: ./commitlint.config.cjs
